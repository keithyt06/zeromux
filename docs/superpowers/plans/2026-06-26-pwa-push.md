# PWA + Web Push 推送通知 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** zeromux 在交互 turn 完成(久候+不在前台时)、无人值守 run 失败/超时、确认队列新增时,向用户所有已订阅设备发 Web Push,息屏可达,点击深链到对应 session。

**Architecture:** 新增 `src/push.rs`(VAPID 密钥 + SQLite 订阅存储 + send_push:web-push 加密 + 现有 reqwest 发送)。SessionManager 持 `Option<Arc<PushService>>` handle(仿 `scheduled`),fan-out 在 settle 分支/run finalize 处 fire-and-forget spawn 推送。前端新增 `sw.js`(push 分级过滤+前台抑制+深链)和 `push.ts`(注册/订阅/状态),设置区两档开关。

**Tech Stack:** Rust/Axum(`cargo test` 内联 `#[cfg(test)]`)、`web-push 0.11`(仅加密+VAPID)、`p256 0.13`(密钥生成)、现有 reqwest 0.12 rustls;React 19 + Vite + Vitest;浏览器原生 Service Worker / Push / Notification API。

## Global Constraints

- 差分方案 C:用 `web-push` 做 RFC8188 加密 + VAPID 签名,取 `WebPushMessage.payload.{content, crypto_headers, content_encoding}` 用**现有 reqwest** POST;**不引入 isahc/hyper**。`web-push = "0.11"` 带 `default-features = false`。
- VAPID 密钥:`p256` 生成 P-256,持久化 `~/.zeromux/vapid.json`(权限 0600);`to_pkcs8_pem()` 出 PKCS8 PEM 喂 `VapidSignatureBuilder::from_pem`(已核实兼容)。
- 推送 fire-and-forget:`tokio::spawn`,失败只 `tracing::warn`,**绝不阻塞 fan-out 热路径,绝不破坏广播扇出不变量**。
- handle 取用:锁内只 `clone()` Arc、锁外用(照抄 `scheduled` 模式 `session_manager.rs:577`),绝不持锁 await。
- turn_done 触发条件:fan-out boundary 块 `if boundary_count >= turn_seq`(settle,`:2008`)分支 + `active_run_id` 为 None;仅 turn 时长 > 60s;per-session 30s 去抖;默认关。
- run_failed:`active_run_id.take()` 的 failed 分支(`:2016`),`active_run_id.is_some()`。
- confirm:reconcile 后查本次新入队行(JOIN config side_effects=1),重启批量合并一条。
- 通知不含对话正文;只 session/任务名 + 类型 + 一个判别维度(failure_kind / 中断原因)。
- 订阅 endpoint SSRF 校验:仅 `https://` 且非私网/环回。
- 前台抑制只对 turn_done(规避 Chrome 静默推送吊销权限);run_failed/confirm 永远弹。
- tag:turn_done=`session_id`;run_failed/confirm=`${session_id}:${kind}`(不被覆盖)。
- 提交信息体尾部:`Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- 后端测试 `cargo test`;前端 `cd frontend && npm test`。

---

## File Structure

- `Cargo.toml` — 加 web-push、p256 依赖。
- `src/push.rs`(新)— Vapid(密钥)、PushStore(SQLite CRUD + SSRF 校验)、PushPayload、PushService(send_push + 去抖)。
- `src/db.rs` 或 push.rs 内 — `push_subscriptions` 表建表。
- `src/web.rs` — 3 个 push 端点。
- `src/session_manager.rs` — push handle 字段 + set_push + 三触发点中的 turn_done/run_failed。
- `src/scheduled_tasks.rs` — confirm 队列入队查询方法 + reconcile 后推送。
- `src/main.rs`(或启动处)— 构建 PushService 并 set 到 SessionManager。
- `frontend/public/sw.js`(新)— service worker。
- `frontend/src/lib/push.ts`(新)— 订阅/状态/纯函数。
- `frontend/src/lib/__tests__/push.test.ts`(新)— 前端单测。
- `frontend/src/main.tsx` — 注册 SW + 深链消息监听。
- 设置区组件(实现时定位)— 两档开关 + iOS 提示。

---

### Task 1: 依赖 + VAPID 密钥管理

**Files:**
- Modify: `Cargo.toml`
- Create: `src/push.rs`(本任务先放 Vapid 部分 + mod 声明)
- Modify: `src/main.rs` 或 `src/lib.rs`(加 `mod push;`)
- Test: `src/push.rs` 内 `#[cfg(test)]`

**Interfaces:**
- Produces: `pub struct Vapid { pub pkcs8_pem: String, pub public_key_b64url: String }`;`pub fn load_or_generate_vapid(dir: &Path) -> Result<Vapid, String>`(dir 通常 `~/.zeromux`)。

- [ ] **Step 1: 加依赖**

`Cargo.toml` 的 `[dependencies]` 加:
```toml
web-push = { version = "0.11", default-features = false }
p256 = { version = "0.13", features = ["pem", "ecdsa"] }
```

- [ ] **Step 2: 写失败测试**

`src/push.rs` 新建,末尾加测试模块:
```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::fs;

    #[test]
    fn vapid_load_or_generate_idempotent() {
        let dir = std::env::temp_dir().join(format!("zmx-vapid-test-{}", std::process::id()));
        fs::create_dir_all(&dir).unwrap();
        let _ = fs::remove_file(dir.join("vapid.json"));
        let a = load_or_generate_vapid(&dir).unwrap();
        let b = load_or_generate_vapid(&dir).unwrap();  // 第二次应读回同一对
        assert_eq!(a.pkcs8_pem, b.pkcs8_pem);
        assert_eq!(a.public_key_b64url, b.public_key_b64url);
        assert!(a.pkcs8_pem.contains("BEGIN PRIVATE KEY"));  // PKCS8
        assert!(!a.public_key_b64url.contains('+') && !a.public_key_b64url.contains('/'));  // base64url 无 +/
        assert!(a.public_key_b64url.len() > 80);  // uncompressed P-256 point = 65 bytes → ~87 b64url chars
        fs::remove_dir_all(&dir).ok();
    }
}
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test --lib push::tests::vapid 2>&1 | tail -15`
Expected: 编译失败 `cannot find function load_or_generate_vapid`。

- [ ] **Step 4: 实现 Vapid**

`src/push.rs` 顶部(测试模块之前):
```rust
use std::path::Path;
use base64::Engine;

#[derive(Clone)]
pub struct Vapid {
    pub pkcs8_pem: String,        // 私钥 PKCS8 PEM,喂 web-push from_pem
    pub public_key_b64url: String, // uncompressed point base64url,给前端 applicationServerKey
}

#[derive(serde::Serialize, serde::Deserialize)]
struct VapidFile { pkcs8_pem: String, public_key_b64url: String }

/// 读 ~/.zeromux/vapid.json;不存在则用 p256 生成 P-256 对、落盘(0600)。
pub fn load_or_generate_vapid(dir: &Path) -> Result<Vapid, String> {
    let path = dir.join("vapid.json");
    if let Ok(bytes) = std::fs::read(&path) {
        if let Ok(vf) = serde_json::from_slice::<VapidFile>(&bytes) {
            return Ok(Vapid { pkcs8_pem: vf.pkcs8_pem, public_key_b64url: vf.public_key_b64url });
        }
    }
    // 生成
    use p256::ecdsa::SigningKey;
    use p256::pkcs8::EncodePrivateKey;
    let mut rng = rand::rngs::OsRng;
    let sk = SigningKey::random(&mut rng);
    let pkcs8_pem = sk.to_pkcs8_pem(p256::pkcs8::LineEnding::LF)
        .map_err(|e| format!("pkcs8 pem: {e}"))?.to_string();
    // 公钥 uncompressed point (0x04 || X || Y) → base64url no-pad
    let vk = sk.verifying_key();
    let point = vk.to_encoded_point(false); // uncompressed
    let public_key_b64url = base64::engine::general_purpose::URL_SAFE_NO_PAD.encode(point.as_bytes());

    let vf = VapidFile { pkcs8_pem: pkcs8_pem.clone(), public_key_b64url: public_key_b64url.clone() };
    std::fs::create_dir_all(dir).map_err(|e| format!("mkdir: {e}"))?;
    let json = serde_json::to_vec_pretty(&vf).map_err(|e| format!("ser: {e}"))?;
    std::fs::write(&path, &json).map_err(|e| format!("write: {e}"))?;
    #[cfg(unix)]
    {
        use std::os::unix::fs::PermissionsExt;
        let _ = std::fs::set_permissions(&path, std::fs::Permissions::from_mode(0o600));
    }
    Ok(Vapid { pkcs8_pem, public_key_b64url })
}
```
若 `mod push;` 未声明,在 `src/main.rs`(或 lib root)加 `mod push;`。

- [ ] **Step 5: 运行确认通过**

Run: `cargo test --lib push::tests::vapid 2>&1 | tail -15`
Expected: PASS。(若 p256 API 细节有出入——如 `to_encoded_point`/`OsRng` 路径——按编译器提示修正,语义不变。)

- [ ] **Step 6: 提交**

```bash
git add Cargo.toml Cargo.lock src/push.rs src/main.rs
git commit -m "feat(push): VAPID keypair load-or-generate (p256, persisted 0600)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: PushStore(SQLite 表 + CRUD + SSRF 校验)

**Files:**
- Modify: `src/push.rs`
- Test: `src/push.rs` `#[cfg(test)]`

**Interfaces:**
- Consumes: 现有 db 层(rusqlite `Connection`,经 `src/db.rs` 的模式;核实 `grep -n "CREATE TABLE\|fn migrate\|Connection" src/db.rs` 看建表/连接惯例)。
- Produces:
  - `pub struct Subscription { pub endpoint: String, pub p256dh: String, pub auth: String }`
  - `pub struct PushStore { /* holds db handle/pool like other stores */ }`
  - `impl PushStore`:`pub fn upsert(&self, user_id, endpoint, p256dh, auth) -> Result<(),String>`、`pub fn list_for_user(&self, user_id) -> Vec<Subscription>`、`pub fn delete(&self, endpoint)`。
  - `pub fn endpoint_is_safe(endpoint: &str) -> bool`(SSRF 校验,纯函数)。

- [ ] **Step 1: 核实 db 模式**

Run: `grep -n "CREATE TABLE\|fn new\|Connection\|Mutex<Connection>\|rusqlite\|Pool" src/db.rs src/session_store.rs | head -20`
据此决定 PushStore 持有什么(单 `Mutex<Connection>` 还是 pool),与现有 store 一致。

- [ ] **Step 2: 写失败测试**

```rust
    #[test]
    fn endpoint_ssrf_validation() {
        assert!(endpoint_is_safe("https://fcm.googleapis.com/fcm/send/abc"));
        assert!(endpoint_is_safe("https://updates.push.services.mozilla.com/wpush/v2/x"));
        assert!(!endpoint_is_safe("http://fcm.googleapis.com/x"));   // 非 https
        assert!(!endpoint_is_safe("https://localhost/x"));
        assert!(!endpoint_is_safe("https://127.0.0.1/x"));
        assert!(!endpoint_is_safe("https://10.0.0.5/x"));
        assert!(!endpoint_is_safe("https://192.168.1.1/x"));
        assert!(!endpoint_is_safe("https://[::1]/x"));
        assert!(!endpoint_is_safe("not a url"));
    }

    #[test]
    fn push_store_upsert_list_delete() {
        let store = PushStore::open_in_memory().unwrap();  // 测试用内存 db
        store.upsert("u1", "https://ep/a", "p1", "a1").unwrap();
        store.upsert("u1", "https://ep/b", "p2", "a2").unwrap();
        store.upsert("u1", "https://ep/a", "p1b", "a1b").unwrap(); // 同 endpoint upsert
        store.upsert("u2", "https://ep/c", "p3", "a3").unwrap();
        let mut u1 = store.list_for_user("u1");
        u1.sort_by(|a,b| a.endpoint.cmp(&b.endpoint));
        assert_eq!(u1.len(), 2);                       // a(更新后) + b,不重复
        assert_eq!(u1[0].p256dh, "p1b");               // upsert 覆盖
        store.delete("https://ep/a");
        assert_eq!(store.list_for_user("u1").len(), 1);
    }
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test --lib push::tests 2>&1 | tail -15`
Expected: 编译失败(PushStore/endpoint_is_safe 未定义)。

- [ ] **Step 4: 实现**

```rust
pub struct Subscription { pub endpoint: String, pub p256dh: String, pub auth: String }

pub fn endpoint_is_safe(endpoint: &str) -> bool {
    let url = match url::Url::parse(endpoint) { Ok(u) => u, Err(_) => return false };
    if url.scheme() != "https" { return false; }
    let host = match url.host_str() { Some(h) => h, None => return false };
    // 拒环回/私网/链路本地
    if host.eq_ignore_ascii_case("localhost") { return false; }
    if let Ok(ip) = host.parse::<std::net::IpAddr>() {
        match ip {
            std::net::IpAddr::V4(v4) => {
                if v4.is_loopback() || v4.is_private() || v4.is_link_local() || v4.is_unspecified() { return false; }
            }
            std::net::IpAddr::V6(v6) => {
                if v6.is_loopback() || v6.is_unspecified() { return false; }
            }
        }
    }
    // 形如 [::1] 的会被 host_str 去括号成 ::1,上面已覆盖;非 IP 主机名放行
    true
}
```
PushStore:照 §Step1 核实的 db 模式实现。建表 SQL(在 PushStore 构造或 migrate 处执行):
```sql
CREATE TABLE IF NOT EXISTS push_subscriptions (
    endpoint   TEXT PRIMARY KEY,
    user_id    TEXT NOT NULL,
    p256dh     TEXT NOT NULL,
    auth       TEXT NOT NULL,
    created_ms INTEGER NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_push_user ON push_subscriptions(user_id);
```
`upsert` 用 `INSERT INTO ... ON CONFLICT(endpoint) DO UPDATE SET user_id=,p256dh=,auth=,created_ms=`。`open_in_memory()` 测试辅助:`Connection::open_in_memory()` + 建表。created_ms 用传入的时钟或 `0`(测试可固定)。
> `url` crate:若 Cargo.toml 没有,reqwest 已传递依赖 url,但显式用需加 `url = "2"`。核实 `grep '^url' Cargo.toml`,无则加。

- [ ] **Step 5: 运行确认通过**

Run: `cargo test --lib push::tests 2>&1 | tail -15`
Expected: 2 测试 PASS。

- [ ] **Step 6: 提交**

```bash
git add Cargo.toml Cargo.lock src/push.rs
git commit -m "feat(push): PushStore (SQLite subscriptions) + SSRF endpoint guard

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: PushPayload 文案 + send_push(加密+发送+失效删除)

**Files:**
- Modify: `src/push.rs`
- Test: `src/push.rs` `#[cfg(test)]`

**Interfaces:**
- Consumes: Task1 `Vapid`、Task2 `PushStore`/`Subscription`;现有 `reqwest::Client`。
- Produces:
  - `pub struct PushPayload { pub kind: String, pub session_id: String, pub title: String, pub body: String }`
  - `pub fn payload_for(kind: &str, name: &str, session_id: &str, failure_kind: Option<&str>) -> PushPayload`(文案构造,纯函数)
  - `pub fn confirm_batch_payload(n: usize) -> PushPayload`(重启批量合并)
  - `pub struct PushService { /* vapid + store + reqwest client + 去抖 map */ }`
  - `impl PushService`:`pub async fn send_to_user(&self, user_id: &str, payload: &PushPayload)`(fire-and-forget 调用方 spawn)。
  - send 结果可注入以测 410 删除:把"单次发送"抽成 `async fn deliver(&self, sub, body, headers) -> DeliveryOutcome { Ok, Gone, Err }`,send_to_user 据 `Gone` 调 store.delete。测试用一个返回固定 outcome 的路径(见下)。

- [ ] **Step 1: 写失败测试**(文案 + 410 删除逻辑)

```rust
    #[test]
    fn payload_text_by_kind() {
        let t = payload_for("turn_done", "重构会话", "s1", None);
        assert!(t.title.contains("重构会话") && t.title.contains("完成"));
        let f = payload_for("run_failed", "夜跑", "s2", Some("idle_timeout"));
        assert!(f.title.contains("失败"));
        assert!(f.body.contains("空闲") || f.body.contains("超时"));  // failure_kind 中文
        let c = payload_for("confirm", "备份任务", "s3", Some("watchdog_timeout"));
        assert!(c.title.contains("需确认"));
        assert!(c.body.contains("中断"));  // 含中断原因(评审 B2)
        let batch = confirm_batch_payload(3);
        assert!(batch.title.contains("3") && batch.title.contains("确认"));
    }

    // 410 删除:把发送结果建模为 DeliveryOutcome,验证 Gone 触发 delete。
    #[test]
    fn gone_outcome_removes_subscription() {
        let store = PushStore::open_in_memory().unwrap();
        store.upsert("u1", "https://ep/gone", "p", "a").unwrap();
        // 直接验证"处理一个 Gone outcome"的纯逻辑:
        handle_delivery_outcome(&store, "https://ep/gone", DeliveryOutcome::Gone);
        assert_eq!(store.list_for_user("u1").len(), 0);
        store.upsert("u1", "https://ep/ok", "p", "a").unwrap();
        handle_delivery_outcome(&store, "https://ep/ok", DeliveryOutcome::TransientErr);
        assert_eq!(store.list_for_user("u1").len(), 1);  // 非 Gone 不删
    }
```

- [ ] **Step 2: 运行确认失败**

Run: `cargo test --lib push::tests::payload 2>&1 | tail; cargo test --lib push::tests::gone 2>&1 | tail`
Expected: 编译失败。

- [ ] **Step 3: 实现 payload + outcome 处理 + send**

```rust
pub struct PushPayload { pub kind: String, pub session_id: String, pub title: String, pub body: String }

fn failure_kind_zh(fk: Option<&str>) -> &'static str {
    match fk {
        Some("idle_timeout") => "因空闲超时中断",
        Some("watchdog_timeout") => "因运行超时中断",
        Some("orphaned_restart") => "因重启中断",
        Some("cli_error") => "传输错误",
        Some("cli_exited") => "进程退出",
        _ => "运行中断",
    }
}

pub fn payload_for(kind: &str, name: &str, session_id: &str, fk: Option<&str>) -> PushPayload {
    let (title, body) = match kind {
        "turn_done"  => (format!("✅ {name} 完成"), "本轮已结束".to_string()),
        "run_failed" => (format!("⚠️ {name} 失败"), failure_kind_zh(fk).trim_start_matches('因').to_string()),
        "confirm"    => (format!("❓ {name} 需确认"), format!("{},等待确认", failure_kind_zh(fk))),
        _            => (name.to_string(), String::new()),
    };
    PushPayload { kind: kind.to_string(), session_id: session_id.to_string(), title, body }
}

pub fn confirm_batch_payload(n: usize) -> PushPayload {
    PushPayload { kind: "confirm".into(), session_id: String::new(),
        title: format!("❓ {n} 个任务待确认"), body: "重启后需逐一确认".into() }
}

#[derive(PartialEq, Debug)]
pub enum DeliveryOutcome { Ok, Gone, TransientErr }

pub fn handle_delivery_outcome(store: &PushStore, endpoint: &str, o: DeliveryOutcome) {
    if o == DeliveryOutcome::Gone { store.delete(endpoint); }
}
```
`PushService`:持 `Vapid`、`Arc<PushStore>`、`reqwest::Client`、`Mutex<HashMap<(String,String), i64>>`(去抖 last_turn_push_ms,键 (user,session))。
- `send_to_user(user_id, payload)`:`store.list_for_user` → 每个 sub:
  - `SubscriptionInfo::new(&sub.endpoint, &sub.p256dh, &sub.auth)`
  - `WebPushMessageBuilder::new(&info)`,`set_payload(ContentEncoding::Aes128Gcm, json_bytes)`,`set_vapid_signature(VapidSignatureBuilder::from_pem(self.vapid.pkcs8_pem.as_bytes(), &info)?.build()?)`
  - `.build()?` → `msg.payload`:取 `content`(Vec<u8>)+ `crypto_headers`(Vec<(&str,String)>)+ `content_encoding`
  - reqwest POST 到 `sub.endpoint`:body=content,headers 加 crypto_headers + `Content-Encoding: aes128gcm` + `TTL: 86400` + `Urgency`(turn_done=low,其余=high)
  - 据 HTTP status → `DeliveryOutcome`(404/410→Gone,2xx→Ok,其余→TransientErr)→ `handle_delivery_outcome`
  - 每个 sub 独立 try,错误 `tracing::warn` 不传播。
- 去抖(turn_done):send_to_user 入口若 kind=="turn_done",检查/更新去抖 map,30s 内同 (user,session) 跳过。

- [ ] **Step 4: 运行确认通过**

Run: `cargo test --lib push::tests 2>&1 | tail -15`
Expected: 全 PASS(payload + gone + 前两任务)。
> send_to_user 的真实网络发送不单测(无 mock server);其正确性靠 payload/outcome 纯逻辑测试 + Task9 真机。

- [ ] **Step 5: 提交**

```bash
git add src/push.rs
git commit -m "feat(push): PushPayload text + send_to_user (web-push encrypt, reqwest send, 410 prune)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: REST 端点(vapid-key / subscribe / unsubscribe)

**Files:**
- Modify: `src/web.rs`(authed `/api/*` 组)
- Modify: `src/web.rs` 的 AppState(若 PushService 经 state 暴露)
- Test: `src/web.rs` 或 push.rs(handler 薄,主要靠 endpoint_is_safe 已测 + 手测)

**Interfaces:**
- Consumes: `PushService`(vapid 公钥 + store)、Task2 `endpoint_is_safe`、`CurrentUser`。
- Produces: 路由 `GET /api/push/vapid-key`、`POST /api/push/subscribe`、`POST /api/push/unsubscribe`。

- [ ] **Step 1: 核实 AppState 与路由组**

Run: `grep -n "struct AppState\|\.route(\"/api\|sessions: \|scheduled" src/web.rs | head -20`
确认 PushService 怎么进 AppState(可能 `Arc<PushService>` 或经 SessionManager 暴露)。

- [ ] **Step 2: 实现端点**(无独立单元测试 — handler 薄;逻辑已在 Task2/3 测过)

在 authed `/api/*` 组加路由 + handler:
```rust
async fn push_vapid_key(State(state): State<Arc<AppState>>) -> Result<Json<serde_json::Value>, StatusCode> {
    match state.push.as_ref() {
        Some(p) => Ok(Json(serde_json::json!({ "key": p.vapid_public_key() }))),
        None => Err(StatusCode::SERVICE_UNAVAILABLE),  // 推送禁用(无 VAPID)
    }
}

#[derive(serde::Deserialize)]
struct SubscribeReq { endpoint: String, keys: SubKeys }
#[derive(serde::Deserialize)]
struct SubKeys { p256dh: String, auth: String }

async fn push_subscribe(
    State(state): State<Arc<AppState>>,
    user: axum::Extension<CurrentUser>,
    Json(req): Json<SubscribeReq>,
) -> Result<Json<serde_json::Value>, StatusCode> {
    let p = state.push.as_ref().ok_or(StatusCode::SERVICE_UNAVAILABLE)?;
    if !crate::push::endpoint_is_safe(&req.endpoint) { return Err(StatusCode::BAD_REQUEST); }
    p.store().upsert(&user.id, &req.endpoint, &req.keys.p256dh, &req.keys.auth)
        .map_err(|_| StatusCode::INTERNAL_SERVER_ERROR)?;
    Ok(Json(serde_json::json!({ "ok": true })))
}

#[derive(serde::Deserialize)]
struct UnsubReq { endpoint: String }
async fn push_unsubscribe(
    State(state): State<Arc<AppState>>,
    _user: axum::Extension<CurrentUser>,
    Json(req): Json<UnsubReq>,
) -> Result<Json<serde_json::Value>, StatusCode> {
    if let Some(p) = state.push.as_ref() { p.store().delete(&req.endpoint); }
    Ok(Json(serde_json::json!({ "ok": true })))
}
```
路由(authed 组):
```rust
.route("/api/push/vapid-key", get(push_vapid_key))
.route("/api/push/subscribe", post(push_subscribe))
.route("/api/push/unsubscribe", post(push_unsubscribe))
```
PushService 需暴露 `vapid_public_key(&self) -> String` 和 `store(&self) -> &PushStore`(在 push.rs 加这两个 getter)。

- [ ] **Step 3: 编译 + 现有测试不回归**

Run: `cargo build 2>&1 | tail -5 && cargo test --lib 2>&1 | tail -8`
Expected: 编译通过;现有测试全过。

- [ ] **Step 4: 提交**

```bash
git add src/web.rs src/push.rs
git commit -m "feat(push): REST endpoints vapid-key/subscribe/unsubscribe (SSRF-checked)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: SessionManager push handle + 三触发点接线

**Files:**
- Modify: `src/session_manager.rs`(handle 字段 + set_push + boundary 块 turn_done/run_failed)
- Modify: `src/scheduled_tasks.rs`(confirm 队列入队查询 + reconcile 后推送)
- Modify: `src/main.rs`(构建 PushService 并 set)
- Test: `src/scheduled_tasks.rs`(confirm 入队查询)+ `src/push.rs`(去抖/久候纯逻辑,若未在 T3 覆盖则补)

**Interfaces:**
- Consumes: Task3 `PushService::send_to_user`、`payload_for`、`confirm_batch_payload`。
- Produces: `SessionManager` 新 `push: Mutex<Option<Arc<PushService>>>` + `set_push(Arc<PushService>)`;`ScheduledStore` 新 `newly_queued_confirmations_after_reconcile(...) -> Vec<(run_id, owner_id, task_name, failure_kind)>`。

- [ ] **Step 1: 核实 confirm 队列谓词 + reconcile 位置**

Run: `grep -n "confirmation_queue\|fn reconcile_orphans\|fn reconcile_timeouts\|set_run_state\|side_effects" src/scheduled_tasks.rs | head -20`
读 `confirmation_queue` 的 SQL 谓词(aborted + failure_kind IN (...) + confirm_status NULL + side_effects=1),据此写"本次新增"查询。

- [ ] **Step 2: 写失败测试(confirm 入队查询 + turn_done 去抖/久候)**

confirm 测试(scheduled_tasks.rs 测试模块,沿用其现有测试建 store 的方式):
```rust
    #[test]
    fn newly_queued_confirmations_only_side_effecting() {
        let store = test_store();  // 沿用现有测试辅助
        // 造两个 aborted+watchdog_timeout 的 run:一个 side_effects=1 一个 0
        // (按现有 insert config/run 的测试辅助)
        // ... 略:用现有 helper 建 task A(side_effects=1)、B(side_effects=0),各跑一个 run 并 abort
        let newly = store.newly_queued_confirmations(&["runA","runB"]);
        let ids: Vec<_> = newly.iter().map(|c| c.0.as_str()).collect();
        assert!(ids.contains(&"runA"));    // side_effects=1 → 入队
        assert!(!ids.contains(&"runB"));   // side_effects=0 → 不入队
    }
```
turn_done 去抖/久候(push.rs,纯逻辑函数 `should_push_turn_done(now_ms, last_ms, turn_dur_ms) -> bool`):
```rust
    #[test]
    fn turn_done_debounce_and_threshold() {
        // 久候门槛 60s + 去抖 30s
        assert!(!should_push_turn_done(100_000, None, 5_000));     // turn 仅5s < 60s 不推
        assert!(should_push_turn_done(100_000, None, 70_000));     // 70s 久候 推
        assert!(!should_push_turn_done(100_000, Some(90_000), 70_000)); // 距上次 10s < 30s 去抖
        assert!(should_push_turn_done(200_000, Some(90_000), 70_000));  // 距上次 110s 推
    }
```

- [ ] **Step 3: 运行确认失败**

Run: `cargo test --lib newly_queued 2>&1 | tail; cargo test --lib turn_done_debounce 2>&1 | tail`
Expected: 编译失败。

- [ ] **Step 4: 实现**

4a. push.rs 加纯函数:
```rust
pub fn should_push_turn_done(now_ms: i64, last_push_ms: Option<i64>, turn_dur_ms: i64) -> bool {
    if turn_dur_ms < 60_000 { return false; }            // 久候门槛
    match last_push_ms { Some(l) if now_ms - l < 30_000 => false, _ => true }  // 30s 去抖
}
```
(send_to_user 的 turn_done 路径用它 + 内部去抖 map 记 last。)

4b. SessionManager 加字段(`:267` scheduled 旁):`push: Mutex<Option<Arc<crate::push::PushService>>>`,构造初始化 `Mutex::new(None)`,加 `pub fn set_push(&self, p: Arc<...>) { *self.push.lock().unwrap() = Some(p); }` 和取用 `fn push_handle(&self) -> Option<Arc<...>> { self.push.lock().unwrap().clone() }`。

4c. boundary 块(`session_manager.rs:2008-2029`):
- **turn_done**:在 `if boundary_count >= turn_seq { local_running = false; }` 内、且 `active_run_id` 为 None 时,clone session name(`if let Some(m)=mgr.upgrade()` 后 `sessions.lock` 读 name),spawn:
```rust
if boundary_count >= turn_seq && active_run_id.is_none() {
    if let Some(m) = mgr.upgrade() {
        if let Some(p) = m.push_handle() {
            let now = now_millis();
            let dur = run_started_ms.map(|s| now - s).unwrap_or(0);
            let name = m.session_name(&sid).unwrap_or_default();  // 加一个读 name 的辅助
            let (uid, sid2) = (owner_id.clone(), sid.clone());
            tokio::spawn(async move {
                if crate::push::should_push_turn_done(now, p.last_turn_push(&uid,&sid2), dur) {
                    p.mark_turn_pushed(&uid,&sid2,now);
                    p.send_to_user(&uid, &crate::push::payload_for("turn_done",&name,&sid2,None)).await;
                }
            });
        }
    }
}
```
- **run_failed**:在 `active_run_id.take()` 的 `AcpEvent::Error`/`Exit`(failed)分支,spawn `send_to_user(run_failed, failure_kind)`。owner_id/name 同上 clone。
> 注意:turn_done 的判断要在 `active_run_id.take()` **之前**读 `active_run_id.is_none()`(此刻还没 take)。把 turn_done spawn 放在 take 之前的 settle 块里。

4d. scheduled_tasks.rs:`newly_queued_confirmations(run_ids: &[&str]) -> Vec<(String,String,String,Option<String>)>`:对给定 run_ids 跑完整队列谓词(JOIN config side_effects=1)返回命中的。reconcile_orphans/reconcile_timeouts 在翻状态后收集本次受影响 run_ids → 调此查询 → 若 >1 条用 `confirm_batch_payload(n)` spawn 一条,否则逐条 `payload_for("confirm", task_name, "", failure_kind)`。需 ScheduledStore 能拿到 PushService handle(构造时注入 `Option<Arc<PushService>>` 或经回调)。

4e. main.rs:启动构建 `PushService`(load_or_generate_vapid + PushStore + reqwest client),`Arc::new`,`session_manager.set_push(push.clone())`,scheduled store 也注入。VAPID 生成失败 → log warn,不 set(推送禁用)。

- [ ] **Step 5: 运行确认通过 + 编译**

Run: `cargo test --lib 2>&1 | tail -10 && cargo build 2>&1 | tail -5`
Expected: 新测试 PASS,全量不回归,编译通过。

- [ ] **Step 6: 提交**

```bash
git add src/session_manager.rs src/scheduled_tasks.rs src/main.rs src/push.rs
git commit -m "feat(push): wire 3 triggers (turn_done settle / run_failed / confirm queue)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---
### Task 6: 前端 push.ts(纯函数 + 订阅 lib)

**Files:**
- Create: `frontend/src/lib/push.ts`
- Create: `frontend/src/lib/__tests__/push.test.ts`

**Interfaces:**
- Produces:
  - `vapidKeyToUint8Array(b64url: string): Uint8Array`(纯函数)
  - `getPushState(): 'unsupported'|'denied'|'enabled'|'disabled'`(读 Notification.permission + 订阅状态)
  - `levelAllows(kind: string, levels: {important:boolean,routine:boolean}): boolean`(纯函数)
  - `shouldSuppress(visibleActives: string[], payloadSessionId: string): boolean`(纯函数,SW 也复用此逻辑)
  - `enablePush()` / `disablePush()`（副作用,不单测）

- [ ] **Step 1: 写失败测试**

`frontend/src/lib/__tests__/push.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { vapidKeyToUint8Array, levelAllows, shouldSuppress } from '../push'

describe('push pure fns', () => {
  it('vapidKeyToUint8Array decodes base64url to 65-byte P-256 point', () => {
    // 65 字节 uncompressed point 的 base64url(0x04 + 32 + 32),用一个已知长度向量
    const b64url = 'B' + 'A'.repeat(86)  // 87 chars ≈ 65 bytes
    const arr = vapidKeyToUint8Array(b64url)
    expect(arr).toBeInstanceOf(Uint8Array)
    expect(arr.length).toBe(65)
  })
  it('levelAllows: routine off blocks turn_done, important always on', () => {
    expect(levelAllows('turn_done', { important: true, routine: false })).toBe(false)
    expect(levelAllows('turn_done', { important: true, routine: true })).toBe(true)
    expect(levelAllows('run_failed', { important: true, routine: false })).toBe(true)
    expect(levelAllows('confirm', { important: false, routine: false })).toBe(true) // 重要恒弹? 见实现注
  })
  it('shouldSuppress: visible client on same session suppresses', () => {
    expect(shouldSuppress(['s1','s2'], 's1')).toBe(true)
    expect(shouldSuppress(['s2'], 's1')).toBe(false)
    expect(shouldSuppress([], 's1')).toBe(false)
  })
})
```
> 实现注:`levelAllows` 中 important 类(run_failed/confirm)恒返回 true(总弹,不受 routine 影响);仅 turn_done 受 routine 开关。confirm 在 important=false 时是否还弹——按 spec §5.4「重要默认开」语义,important 开关控制 run_failed+confirm;若用户关了 important 则不弹。**修正测试**:`levelAllows('confirm', {important:false,...})` 应为 `false`。实现以 important 控 run_failed+confirm、routine 控 turn_done。

- [ ] **Step 2: 修正测试并运行确认失败**

把上面 confirm 那条断言改为 `toBe(false)`。
Run: `cd frontend && npx vitest run src/lib/__tests__/push.test.ts 2>&1 | tail -15`
Expected: FAIL(模块不存在)。

- [ ] **Step 3: 实现 push.ts**

```ts
export function vapidKeyToUint8Array(b64url: string): Uint8Array {
  const pad = '='.repeat((4 - (b64url.length % 4)) % 4)
  const b64 = (b64url + pad).replace(/-/g, '+').replace(/_/g, '/')
  const raw = atob(b64)
  const arr = new Uint8Array(raw.length)
  for (let i = 0; i < raw.length; i++) arr[i] = raw.charCodeAt(i)
  return arr
}

export type PushLevels = { important: boolean; routine: boolean }

export function levelAllows(kind: string, levels: PushLevels): boolean {
  if (kind === 'turn_done') return levels.routine
  return levels.important  // run_failed / confirm
}

export function shouldSuppress(visibleActiveSessions: string[], payloadSessionId: string): boolean {
  return visibleActiveSessions.includes(payloadSessionId)
}

export type PushState = 'unsupported' | 'denied' | 'enabled' | 'disabled'
export async function getPushState(): Promise<PushState> {
  if (!('PushManager' in window) || !('serviceWorker' in navigator)) return 'unsupported'
  if (Notification.permission === 'denied') return 'denied'
  const reg = await navigator.serviceWorker.getRegistration()
  const sub = await reg?.pushManager.getSubscription()
  return sub ? 'enabled' : 'disabled'
}

export async function enablePush(): Promise<void> {
  const perm = await Notification.requestPermission()
  if (perm !== 'granted') return
  const reg = await navigator.serviceWorker.ready
  const res = await fetch('/api/push/vapid-key')
  const { key } = await res.json()
  const sub = await reg.pushManager.subscribe({
    userVisibleOnly: true,
    applicationServerKey: vapidKeyToUint8Array(key),
  })
  const j = sub.toJSON()
  await fetch('/api/push/subscribe', {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ endpoint: j.endpoint, keys: j.keys }),
  })
}

export async function disablePush(): Promise<void> {
  const reg = await navigator.serviceWorker.getRegistration()
  const sub = await reg?.pushManager.getSubscription()
  if (sub) {
    await fetch('/api/push/unsubscribe', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ endpoint: sub.endpoint }),
    })
    await sub.unsubscribe()
  }
}
```
`getPushState` 是 async — 测试只测三个纯函数,getPushState/enable/disable 不单测。

- [ ] **Step 4: 运行确认通过**

Run: `cd frontend && npx vitest run src/lib/__tests__/push.test.ts 2>&1 | tail -15`
Expected: 3 测试 PASS。

- [ ] **Step 5: lint + 提交**

Run: `cd frontend && npm run lint 2>&1 | tail -5`(新代码无错)
```bash
git add frontend/src/lib/push.ts frontend/src/lib/__tests__/push.test.ts
git commit -m "feat(push): frontend push lib (subscribe + pure fns: vapidKey/levelAllows/shouldSuppress)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Service Worker sw.js

**Files:**
- Create: `frontend/public/sw.js`

**Interfaces:**
- Consumes: 后端推送的 PushPayload JSON;前端页面经 postMessage 上报 active session。
- Produces: SW 文件(push/notificationclick/message 事件)。无单测(SW 全局 API 难单测;抑制/分级纯逻辑已在 push.ts Task6 测过,sw.js 内联同逻辑)。

- [ ] **Step 1: 实现 sw.js**

`frontend/public/sw.js`:
```js
// active session 上报:每个 client 通过 postMessage 告知它当前可见+active 的会话
let clientActives = {}  // clientId -> { sessionId, visible }

self.addEventListener('message', (e) => {
  const d = e.data || {}
  if (d.type === 'active_session' && e.source) {
    clientActives[e.source.id] = { sessionId: d.id, visible: d.visible !== false }
  }
})

function levelAllows(kind, levels) {
  if (kind === 'turn_done') return !!(levels && levels.routine)
  return !levels || levels.important !== false  // 默认 important 开
}

self.addEventListener('push', (event) => {
  event.waitUntil((async () => {
    let payload = {}
    try { payload = event.data ? event.data.json() : {} } catch (_) {}
    const { kind, session_id, title, body } = payload
    // 分级:读 IndexedDB/Cache 里前端写的 push_levels;读不到用默认(important 开 / routine 关)
    const levels = await readLevels()  // { important:true, routine:false } 默认
    if (!levelAllows(kind, levels)) return
    // 前台抑制:仅 turn_done。实时问所有可见 client 的 active
    if (kind === 'turn_done') {
      const wins = await self.clients.matchAll({ type: 'window' })
      const visibleActives = wins
        .filter(c => c.visibilityState === 'visible')
        .map(c => (clientActives[c.id] || {}).sessionId)
        .filter(Boolean)
      if (visibleActives.includes(session_id)) return  // 用户正看着 → 抑制
    }
    const tag = kind === 'turn_done' ? session_id : `${session_id}:${kind}`
    await self.registration.showNotification(title || 'zeromux', {
      body: body || '', tag, data: { session_id },
    })
  })())
})

self.addEventListener('notificationclick', (event) => {
  event.notification.close()
  const sid = (event.notification.data || {}).session_id
  event.waitUntil((async () => {
    const wins = await self.clients.matchAll({ type: 'window', includeUncontrolled: true })
    if (wins.length > 0) {
      await wins[0].focus()
      wins[0].postMessage({ type: 'open_session', id: sid })
    } else {
      await self.clients.openWindow(`/?session=${encodeURIComponent(sid || '')}`)
    }
  })())
})

// push_levels 存取(简单用 Cache API 存一个 JSON 响应)
async function readLevels() {
  try {
    const cache = await caches.open('zmx-push')
    const res = await cache.match('levels')
    if (res) return await res.json()
  } catch (_) {}
  return { important: true, routine: false }
}
```
> push_levels 写入:push.ts 在用户改开关时 `caches.open('zmx-push').then(c=>c.put('levels', new Response(JSON.stringify(levels))))`。这一步加到 Task6 的 push.ts(setLevels 函数)或 Task8 设置组件里。

- [ ] **Step 2: 验证 sw.js 语法**

Run: `cd frontend && npx tsc --noEmit --allowJs --checkJs false public/sw.js 2>&1 | tail -5 || node --check public/sw.js && echo "syntax ok"`
Expected: 语法 OK(`node --check` 通过)。

- [ ] **Step 3: 提交**

```bash
git add frontend/public/sw.js
git commit -m "feat(push): service worker — leveled push, turn_done foreground suppression, deep-link click

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: 注册 SW + active 上报 + 深链 + 设置开关 UI

**Files:**
- Modify: `frontend/src/main.tsx`(注册 SW)
- Modify: `frontend/src/App.tsx`(active session 上报 SW + open_session 深链监听)
- Create/Modify: 设置区组件(两档开关 + iOS 提示)
- Modify: `frontend/src/lib/push.ts`(加 setLevels/getLevels)

**Interfaces:**
- Consumes: Task6 push.ts(enablePush/disablePush/getPushState)、Task7 sw.js。

- [ ] **Step 1: 核实设置 UI 位置 + active session 来源**

Run: `grep -n "activeId\|setActiveId\|settings\|Settings\|menu\|gear\|⚙\|account" frontend/src/App.tsx | head -20`
确定 active session 状态(`activeId`)和设置/菜单挂载点。

- [ ] **Step 2: main.tsx 注册 SW**

`frontend/src/main.tsx` 在 render 前加:
```tsx
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/sw.js').catch(() => { /* 推送非关键,忽略 */ })
}
```

- [ ] **Step 3: App.tsx active 上报 + 深链监听**

active session 变化时上报 SW(在 activeId 的 effect 里):
```tsx
useEffect(() => {
  const sw = navigator.serviceWorker?.controller
  if (sw) sw.postMessage({ type: 'active_session', id: activeId, visible: document.visibilityState === 'visible' })
}, [activeId])
useEffect(() => {
  const onVis = () => navigator.serviceWorker?.controller?.postMessage(
    { type: 'active_session', id: activeId, visible: document.visibilityState === 'visible' })
  document.addEventListener('visibilitychange', onVis)
  return () => document.removeEventListener('visibilitychange', onVis)
}, [activeId])
// 深链:SW 点击通知后 postMessage open_session
useEffect(() => {
  const onMsg = (e: MessageEvent) => {
    if (e.data?.type === 'open_session' && e.data.id) setActiveId(e.data.id)
  }
  navigator.serviceWorker?.addEventListener('message', onMsg)
  return () => navigator.serviceWorker?.removeEventListener('message', onMsg)
}, [])
```
启动时解析 `?session=` query 深链:
```tsx
useEffect(() => {
  const sid = new URLSearchParams(location.search).get('session')
  if (sid) setActiveId(sid)
}, [])
```

- [ ] **Step 4: push.ts 加 levels 存取**

```ts
export async function setLevels(levels: PushLevels): Promise<void> {
  const cache = await caches.open('zmx-push')
  await cache.put('levels', new Response(JSON.stringify(levels)))
  localStorage.setItem('zmx_push_levels', JSON.stringify(levels))
}
export function getLevels(): PushLevels {
  try { return JSON.parse(localStorage.getItem('zmx_push_levels') || '') } catch { return { important: true, routine: false } }
}
```

- [ ] **Step 5: 设置开关组件**

新建或在现有设置区加组件:主开关(enablePush/disablePush,显示 getPushState)+ 两档 toggle(important 默认 true / routine 默认 false,改动调 setLevels)+ iOS 检测提示。iOS 检测:
```tsx
const isIOS = /iP(hone|ad|od)/.test(navigator.userAgent)
const standalone = (navigator as any).standalone === true || window.matchMedia('(display-mode: standalone)').matches
const showIOSHint = isIOS && !standalone
// showIOSHint → 显示"① 点分享按钮 ② 添加到主屏幕"两步静态说明
```
组件渲染:主开关、(已启用时)两档 toggle、(showIOSHint 时)iOS 两步说明、(unsupported 时)禁用+提示。

- [ ] **Step 6: 测试(组件渲染)**

为开关组件写一个轻量渲染测试(跟随现有组件测试风格),至少断言:主开关渲染、iOS 提示在模拟 iOS UA 时出现。Run: `cd frontend && npx vitest run <新测试文件> 2>&1 | tail -10`。

- [ ] **Step 7: lint + 提交**

Run: `cd frontend && npm run lint 2>&1 | tail -5`
```bash
git add frontend/src/main.tsx frontend/src/App.tsx frontend/src/lib/push.ts <设置组件+测试>
git commit -m "feat(push): register SW, report active session, deep-link, two-tier settings toggle

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 9: 全量测试 + 体积记账 + 真机验证

**Files:** 无代码改动(验证)。

- [ ] **Step 1: 后端全量**

Run: `cargo test 2>&1 | tail -8`
Expected: 全绿(含 push 新测 + 既有 209)。

- [ ] **Step 2: 前端全量**

Run: `cd frontend && npm test 2>&1 | tail -12`
Expected: 全绿(含 push.test + 开关组件测试)。

- [ ] **Step 3: 体积记账(评审 P1-3)**

Run: `cargo build --release 2>&1 | tail -3 && ls -l target/release/zeromux | awk '{print $5}'`
对比加 web-push/p256 前的 size(本 feature 前 main 是 ~15411256)。记录增量到本计划末尾。若增量 > 1.5MB,在报告里标注"评估瘦身(仅 ece + jsonwebtoken+p256 自拼 VAPID 头)"作为后续 TODO,本期不阻塞。

- [ ] **Step 4: 真机验证(部署后)**

部署 live 后:
- iOS Safari「添加到主屏幕」→ 打开 PWA → 设置里开启推送 → 授权(应弹系统权限)。
- 「重要」默认开、「日常」默认关核对。
- 锁屏 → 触发后台 run 失败(或手动制造)/ 确认队列 → 收到推送。
- 开启「日常」→ 发个 >60s 的 prompt → 切到别的会话/锁屏 → turn 完成收到推送;在前台看着同会话则不收到(抑制)。
- 点推送 → 聚焦/打开并深链到对应 session。
- Android Chrome + 桌面 Chrome 各验一遍。
记录结果。

- [ ] **Step 5: 标记完成**(纯验证,无提交)。若发现偏差,回对应 Task 修复并补测。

---

## Self-Review

**1. Spec coverage:**
- §3 依赖 → Task1(web-push/p256)✅
- §4.1 VAPID → Task1;PushStore/表 → Task2;PushPayload/send_push/410删除 → Task3 ✅
- §4.2 三端点 + SSRF → Task4(+Task2 endpoint_is_safe)✅
- §4.3 三触发点(turn_done settle+去抖+久候 / run_failed / confirm 入队查询+批量合并)→ Task5 ✅
- §5.1 sw.js(分级+实时抑制+tag分级+深链)→ Task7 ✅
- §5.2 push.ts → Task6;§5.3 active 上报 → Task8;§5.4 两档开关+iOS → Task8;§5.5 深链落地 → Task8 ✅
- §6 错误降级 → 贯穿(VAPID失败禁用 Task5e、fire-and-forget Task5c、410删除 Task3、抑制只turn_done Task7)✅
- §7 测试 12 项 → Task1(vapid)/2(ssrf,store)/3(payload,gone)/5(newly_queued,debounce)/6(vapidKey,levelAllows,shouldSuppress);getPushState 状态机靠手测(async,副作用)→ 可接受 ✅
- §8 非目标 → 计划未触碰(成本护栏/Feature3 审批等)✅

**2. Placeholder scan:** 无 TBD。Task2/4/5/8 的"核实"步是要求实现者先 grep 确认现有 db/AppState/谓词/UI 位置(依赖现网代码),给了确切 grep 命令,非占位符。Task8 设置组件路径未写死(因需核实现有 UI 结构),但给了挂载判断方式。

**3. Type consistency:** `payload_for(kind,name,session_id,fk)` Task3 定义、Task5 消费一致;`endpoint_is_safe` Task2 定义、Task4 消费;`should_push_turn_done`/`send_to_user` Task3/5;前端 `levelAllows/shouldSuppress/vapidKeyToUint8Array` Task6 定义、Task7 sw.js 内联同逻辑(故意复制,SW 无法 import bundle)、Task8 消费。PushLevels 形状 `{important,routine}` 全程一致。

**修正记录:** Task6 测试里 confirm 在 important=false 的断言初版写 toBe(true),Step2 已要求改为 toBe(false) 并说明 important 控 run_failed+confirm 的语义。
